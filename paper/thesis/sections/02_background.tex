% 根拠：E1（実装上の定義）、E2（評価対象）。統計の説明も本研究で使う分に絞る。
\section{背景}
\label{sec:background}
背景は提案手法を理解するための準備，関連研究は比較対象の整理として，現時点では別のセクションにしている．
\begin{itemize}
    \item ASK：背景と関連研究を分ける構成で読みやすいか．
\end{itemize}

\subsection{サービスとメトリクスの関係}
\begin{itemize}
    \item サービスは「商品検索」「在庫確認」「決済」のように，システム内で一つの役割を担うプログラムの単位である．一方，メトリクスは各サービスの状態を数値として測る項目であり，時間とともに記録される．
    \item 一つのサービスから複数のメトリクスが得られる．たとえば「決済サービス」というサービスの中には，「CPU使用率」，「メモリ使用率」，「応答時間」などのメトリクスがある．同じ種類のメトリクスでも，どのサービスから得たかは区別する．
    \item 入力は障害前後のメトリクス時系列，出力は原因候補となるサービスの順位である．実験では意図的に不具合を起こしたサービスを正解とし，「どのメトリクスが真の原因か」まで当てる課題とは分ける．
    \item 図案：各サービスと複数のメトリクス→メトリクスごとの分析→サービス単位の候補一覧．正解情報は採点に使い，順位を作る際には使わないことを示す．
\end{itemize}

\subsection{普段の動きを表す自己回帰モデル}
\begin{itemize}
    \item 自己回帰（AR）モデルは，あるメトリクスについて，直前までの値から次の値を予測するモデルである．$x_t=c+\sum_{j=1}^{p}\phi_jx_{t-j}+\varepsilon_t$ を示し，本研究では正常時のデータだけから係数を推定すると説明する．
    \item 一時点先予測では，時点$t$までの実測値を入力し，次の値$\widehat{x}_{t+1\mid t}$を予測する．次の時点では新たに得た実測値$x_{t+1}$を入力できるため，毎回，直近の実測値を出発点にできる．
    \item 本研究が障害後について求めたいのは，「障害が起きず，正常な動きが続いていた場合」の複数時点先予測である．障害後の実測値は予測に入れず，最初の予測値$\widehat{x}_{T+1\mid T}$を使って$\widehat{x}_{T+2\mid T}$を求める，というように再帰的に先へ進む．
    \item 複数時点先予測には，未知の将来の変動が一時点ずつ加わる．そのため，一時点先予測とは予測誤差のばらつきが異なり，予測する距離に応じた分散を考える必要がある．図では「各時点で実測値を使える予測」と「時点$T$以降は予測値を使う予測」を並べる．
    \item ASK：正常時の動きをARで近似する理由と，二種類の予測の違いが伝わるか．必要な定常性などの仮定は，提案手法での使い方と結び付けて書く．
\end{itemize}

\subsection{分布が変わったかをベイズ的に比べる}
\begin{itemize}
    \item 「障害の前後で同じ分布」と「障害の前後で異なる分布」の二つの仮説を用意する．平均・分散を事前分布に関して積分した周辺尤度を比べる，と説明する．
    \item Bayes Factor（ベイズ因子，以下BF）は，二つの仮説の周辺尤度の比である．$\mathrm{BF}_{10}=p(D\mid H_1)/p(D\mid H_0)$ と書き，単に平均値の差を見る方法との違いを示す．
    \item 注意：BFが大きいことは，置いた仮説の下で分布変化を支持するという意味．そのまま「このサービスが原因である確率」にはならない．
\end{itemize}
